{
  "name": "for-rent-quality-gates",
  "private": true,
  "scripts": {
    "test:firebase": "firebase emulators:exec --project for-rent-ci --only firestore,storage \"node --test firebase-tests/*.test.cjs\"",
    "validate:demo": "node scripts/validate-demo-fixture.cjs",
    "scan:secrets": "node scripts/scan-secrets.cjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.18.0",
    "firebase-tools": "^15.28.1"
  },
  "overrides": {
    "@opentelemetry/core": "^2.8.0",
    "uuid": "^11.1.1"
  }
}
